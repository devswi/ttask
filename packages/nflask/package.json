{
  "name": "@swizm/nflask",
  "version": "1.0.0",
  "description": "A decorator based koa router",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build --clean && rm -rf *.tsbuildinfo && tsc --build tsconfig.build.json",
    "watch": "tsc --build --clean && tsc --build tsconfig.build.json --watch"
  },
  "keywords": [
    "decorator",
    "koa",
    "@koa/router"
  ],
  "author": "shiwei93 <stayfocusjs@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@types/glob": "^8.0.1",
    "@types/koa": "^2.13.5",
    "@types/koa__router": "^12.0.0",
    "@types/supertest": "^2.0.12",
    "koa": "^2.14.1",
    "koa-body": "^6.0.1",
    "supertest": "^6.3.3",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "@koa/router": "^12.0.0",
    "glob": "^8.1.0"
  }
}
